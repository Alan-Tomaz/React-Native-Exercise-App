import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  dropdownContainer: {
    padding: 0,
    paddingRight: 20,
    paddingLeft: 20,
  },
  dropdown: {
    height: 50,
    borderColor: "#ccc",
    borderWidth: 1,
    borderRadius: 8,
    paddingHorizontal: 8,
  },
  dropdownResult: {
    marginTop: 20,
    fontSize: 16,
  },
});

export default styles;
